import React from 'react';
import styled from 'styled-components';

export default function AboutClients() {
  return <Wrapper>AboutClients</Wrapper>;
}

const Wrapper = styled.section`
  padding: 60px 0;
  pointer-events: auto;
`;
