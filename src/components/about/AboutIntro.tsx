import React from 'react';
import styled from 'styled-components';

export default function AboutIntro() {
  return <Wrapper>AboutIntro</Wrapper>;
}

const Wrapper = styled.section`
  min-height: 100vh;
`;
